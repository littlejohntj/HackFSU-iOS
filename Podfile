# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

target 'Hack FSU' do
	use_frameworks!
	pod 'Glyptodon', '~> 1.0'
	pod 'KMPlaceholderTextView', '~> 1.0.0'
	pod 'Parse' 
end
